// Phase 2 extraction placeholder:
// Keep style source in the legacy script for now to avoid behavior changes.
// This module exists so we can migrate injectStyles() in small steps next.

export const STYLE_MIGRATION_READY = true;
